import React from 'react';
import {Bot, CalendarDays, Compass, Download, Heart, Loader2, MapPin, RefreshCw, Save, Sparkles, Trash2, WifiOff} from 'lucide-react';
import { useLocation } from 'react-router-dom';
import { html } from '../jsx.js';
import { useGuideStore } from '../store/useGuideStore.js';
import { t } from '../data/i18n.js';
import { places, placeCategories } from '../data/places.js';
import { generateItinerary, generateMultiDayItinerary } from '../utils/itinerary.js';
import { PlaceCard } from '../components/PlaceCard.js';
import { placeForPlacesPage, placesPageAdditionalCards, removedPlaceCardIds } from './Places.js';

export const itineraryDesignImage = '/api/apps/romcWH54d4SR/assets/20230716_111429.jpg';

const itineraryPageCss = `
.itinerary-page{position:relative;isolation:isolate}.itinerary-page:before{content:"";position:fixed;inset:0;z-index:-1;pointer-events:none;background:radial-gradient(circle at 12% 4%,rgba(249,211,96,.24),transparent 16rem),radial-gradient(circle at 100% 24%,rgba(183,77,31,.16),transparent 18rem),linear-gradient(180deg,rgba(255,246,232,.68),transparent 24rem)}.dark .itinerary-page:before{background:radial-gradient(circle at 12% 4%,rgba(249,211,96,.12),transparent 16rem),radial-gradient(circle at 100% 24%,rgba(242,113,63,.15),transparent 18rem),linear-gradient(180deg,rgba(70,37,22,.34),transparent 24rem)}.itinerary-hero-card{position:relative;min-height:clamp(22rem,70vw,31rem);overflow:hidden;border-radius:1.45rem;background:#2b1308;box-shadow:0 24px 58px rgba(77,35,11,.24);color:#fffaf3}.itinerary-hero-card:after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(25,10,3,.03) 0%,rgba(31,13,5,.25) 44%,rgba(27,10,3,.91) 100%),radial-gradient(circle at 8% 10%,rgba(255,216,77,.36),transparent 12rem),radial-gradient(circle at 96% 4%,rgba(255,255,255,.20),transparent 8rem)}.itinerary-hero-image{position:absolute;inset:0;background-size:cover;background-position:center top;transform:scale(1.01);filter:saturate(1.08) contrast(1.04)}.itinerary-hero-content{position:relative;z-index:1;display:flex;min-height:inherit;flex-direction:column;justify-content:flex-end;gap:.72rem;padding:clamp(.9rem,4.5vw,1.65rem);text-shadow:0 2px 22px rgba(0,0,0,.42)}.itinerary-hero-topline{position:absolute;left:clamp(.85rem,4vw,1.35rem);right:clamp(.85rem,4vw,1.35rem);top:clamp(.85rem,4vw,1.35rem);display:flex;align-items:center;justify-content:space-between;gap:.55rem}.itinerary-kicker,.itinerary-offline-pill,.itinerary-quick-stats span{display:inline-flex;align-items:center;gap:.34rem;border:1px solid rgba(255,255,255,.34);border-radius:999px;background:rgba(255,250,242,.18);padding:.35rem .62rem;color:#fff7ec;font-size:.68rem;font-weight:950;letter-spacing:.035em;text-transform:uppercase;backdrop-filter:blur(14px)}.itinerary-offline-pill{max-width:62%;text-transform:none;letter-spacing:0}.itinerary-offline-pill.is-ready{background:#ffd84d;border-color:rgba(255,216,77,.75);color:#261306;text-shadow:none}.itinerary-hero-content h1{max-width:9ch;font-size:clamp(2.4rem,13vw,5.2rem)!important;line-height:.84;letter-spacing:-.07em;text-wrap:balance}.itinerary-hero-content p{max-width:38rem;color:rgba(255,250,243,.93);font-size:.88rem;font-weight:800}.itinerary-quick-stats{display:flex;gap:.42rem;overflow-x:auto;padding:.06rem 0 .12rem;scrollbar-width:none}.itinerary-quick-stats::-webkit-scrollbar,.itinerary-chip-row::-webkit-scrollbar{display:none}.itinerary-quick-stats span:first-child{background:#ffd84d;border-color:rgba(255,216,77,.8);color:#251306;text-shadow:none}.itinerary-builder-card,.itinerary-empty-card,.itinerary-ai-card,.itinerary-saved-card,.itinerary-save-panel{border:1px solid hsl(var(--border)/.62);border-radius:1.15rem;background:hsl(var(--card)/.88);box-shadow:var(--shadow-sm);backdrop-filter:blur(18px)}.itinerary-builder-card{display:grid;gap:.68rem;padding:.7rem}.itinerary-control-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:.52rem}.itinerary-field{display:grid;gap:.24rem;font-size:.68rem;font-weight:950;color:hsl(var(--foreground))}.itinerary-field input,.itinerary-field select,.itinerary-save-panel input,.itinerary-saved-trip input{width:100%;min-height:2.15rem;border:1px solid hsl(var(--border));border-radius:.75rem;background:hsl(var(--card));padding:.35rem .6rem;color:hsl(var(--foreground));font-size:.82rem;font-weight:850}.itinerary-chip-row{display:flex;gap:.38rem;overflow-x:auto;padding:.02rem .02rem .12rem;scrollbar-width:none}.itinerary-chip{flex:0 0 auto;min-height:1.95rem!important;border:1px solid hsl(var(--border));border-radius:999px;background:hsl(var(--muted));padding:.32rem .68rem!important;color:hsl(var(--foreground));font-size:.7rem;font-weight:950;white-space:nowrap}.itinerary-chip.is-selected{border-color:hsl(var(--primary)/.45);background:hsl(var(--primary));color:hsl(var(--primary-foreground));box-shadow:0 8px 18px hsl(var(--primary)/.16)}.itinerary-chip.favorite.is-selected{background:hsl(var(--secondary));color:hsl(var(--secondary-foreground));border-color:hsl(var(--secondary)/.45)}.itinerary-generate-button,.itinerary-map-button,.itinerary-save-button,.itinerary-download-button,.itinerary-ai-button{display:inline-flex;align-items:center;justify-content:center;gap:.38rem;border-radius:.85rem;font-weight:950}.itinerary-generate-button{width:100%;min-height:2.65rem!important;background:linear-gradient(135deg,hsl(var(--primary)),#e4832d);color:hsl(var(--primary-foreground));box-shadow:0 16px 32px hsl(var(--primary)/.22)}.itinerary-empty-card{padding:2rem .95rem;text-align:center}.itinerary-empty-card p{margin-top:.7rem;font-size:1rem;font-weight:950}.itinerary-day-section{display:grid;gap:.68rem}.itinerary-day-section>h2{display:flex;align-items:center;gap:.5rem}.itinerary-day-section>h2 span{display:inline-flex;border-radius:999px;background:#ffd84d;padding:.36rem .78rem;color:#281306;font-size:.78rem;font-weight:1000;box-shadow:0 10px 18px rgba(89,48,15,.12)}.itinerary-place-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(15rem,1fr));gap:.7rem}.itinerary-place-item{display:grid;gap:.38rem}.itinerary-slot-label{width:fit-content;border-radius:999px;background:hsl(var(--primary)/.12);padding:.28rem .6rem;color:hsl(var(--primary));font-size:.68rem;font-weight:1000;box-shadow:0 8px 16px hsl(var(--primary)/.08)}.itinerary-save-panel{display:grid;gap:.52rem;padding:.62rem}.itinerary-save-button{min-height:2.25rem!important;background:hsl(var(--accent));padding:.42rem .7rem!important;color:#1f1305;font-size:.76rem}.itinerary-download-button{min-height:2.25rem!important;border:1px solid hsl(var(--border));background:hsl(var(--card));padding:.42rem .7rem!important;color:hsl(var(--foreground));font-size:.76rem}.itinerary-ai-card,.itinerary-saved-card{padding:.78rem}.itinerary-ai-card h2,.itinerary-saved-card h2{font-size:1rem!important;font-weight:1000}.itinerary-ai-button{min-height:2.4rem!important;background:hsl(var(--accent));padding:.45rem .78rem!important;color:#1f1305;font-size:.78rem}.itinerary-saved-card>h2{display:flex;align-items:center;gap:.45rem}.itinerary-saved-card>p{margin-top:.28rem;color:hsl(var(--muted-foreground));font-size:.72rem}.itinerary-saved-grid{display:grid;gap:.5rem;margin-top:.7rem}.itinerary-saved-link,.itinerary-saved-trip{border-radius:.85rem;background:hsl(var(--muted));padding:.58rem;font-size:.78rem;font-weight:900}.itinerary-saved-trip{display:grid;gap:.45rem}.itinerary-saved-trip span{color:hsl(var(--muted-foreground));font-size:.72rem}@media (min-width:640px){.itinerary-control-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.itinerary-save-panel{grid-template-columns:minmax(0,1fr) 9.5rem auto auto}.itinerary-saved-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media (min-width:768px){.itinerary-hero-content{padding:1.75rem}}`;

export function Itinerary() {
  const lang = useGuideStore(s => s.language), itinerary = useGuideStore(s => s.itinerary), setItinerary = useGuideStore(s => s.setItinerary), saveItinerary = useGuideStore(s => s.saveItinerary), savedItineraries = useGuideStore(s => s.savedItineraries), renameItinerary = useGuideStore(s => s.renameItinerary), deleteItinerary = useGuideStore(s => s.deleteItinerary), favorites = useGuideStore(s => s.favorites), offlineReady = useGuideStore(s => s.offlineReady), setOfflineReady = useGuideStore(s => s.setOfflineReady);
  const storedProfile = useGuideStore(s => s.profile || {}), storedPace = useGuideStore(s => s.pace), storedTransport = useGuideStore(s => s.transport);
  const location = useLocation();
  const guidePlaces = React.useMemo(() => [...places, ...placesPageAdditionalCards].map(placeForPlacesPage).filter(place => place.category !== 'coast' && !removedPlaceCardIds.includes(place.id)), []);
  const visibleCategories = placeCategories.filter(cat => cat !== 'dayTrips' && cat !== 'coast');
  const [mode, setMode] = React.useState(location.state && location.state.multiDay ? 'multi' : 'single'), [tripDays, setTripDays] = React.useState(Number(storedProfile.tripLength && String(storedProfile.tripLength).match(/\d+/)?.[0]) || 3);
  const [style, setStyle] = React.useState(storedProfile.planningGoal === 'authentic' ? 'culture' : 'relaxed'), [pace, setPace] = React.useState(storedPace || 'balanced'), [budget, setBudget] = React.useState(storedProfile.budget || 'mid'), [walking, setWalking] = React.useState(storedProfile.accessibility && storedProfile.accessibility !== 'none' ? 'low' : 'medium'), [start, setStart] = React.useState(storedProfile.accommodationArea || 'Centro'), [transport, setTransport] = React.useState(storedTransport || 'mixed'), [cats, setCats] = React.useState((storedProfile.interests && storedProfile.interests.length ? storedProfile.interests : ['food', 'culture']).filter(cat => visibleCategories.includes(cat))), [mustSee, setMustSee] = React.useState(favorites.slice(0, 3));
  const [tripName, setTripName] = React.useState('Oaxaca trip'), [tripDate, setTripDate] = React.useState(new Date().toISOString().slice(0, 10));
  const [aiGuide, setAiGuide] = React.useState(''), [loading, setLoading] = React.useState(false), [error, setError] = React.useState(null), [success, setSuccess] = React.useState(''), [regenCount, setRegenCount] = React.useState(0);
  const itineraryOptions = (seed) => ({ style, pace, budget, walking, categories: cats.length ? cats : ['food', 'culture'], startLocation: start, transport, mustSee, seed });
  const build = () => {
    const seed = `${Date.now()}-${regenCount + 1}-${Math.random().toString(36).slice(2)}`;
    setRegenCount(c => c + 1);
    setItinerary(mode === 'multi' ? generateMultiDayItinerary(guidePlaces, { ...itineraryOptions(seed), days: tripDays }) : generateItinerary(guidePlaces, itineraryOptions(seed)));
    setAiGuide(''); setError(null); setSuccess('');
  };
  const saveCurrent = () => { if (itinerary.length) { saveItinerary(itinerary.filter(item => item.place.category !== 'coast'), { name: tripName, date: tripDate, userId: 'local' }); setOfflineReady(true); setSuccess(t(lang, 'offlineSaved')); } };
  const toggleCat = (cat) => setCats(cats.includes(cat) ? cats.filter(c => c !== cat) : [...cats, cat]);
  const toggleMust = (id) => setMustSee(mustSee.includes(id) ? mustSee.filter(x => x !== id) : [...mustSee, id]);
  const grouped = itinerary.filter(item => item.place && item.place.category !== 'coast').reduce((acc, item) => { const day = item.day || 1; acc[day] = acc[day] || []; acc[day].push(item); return acc; }, {});
  const downloadPlan = () => {
    const activeItinerary = itinerary.filter(item => item.place && item.place.category !== 'coast');
    const text = `${tripName} - ${tripDate}\nEmergency: 911\n\n${activeItinerary.map(i => `${i.day ? `DAY ${i.day} · ` : ''}${t(lang, i.slot).toUpperCase()}: ${i.place.name}\nArea: ${i.place.area}\nAddress/map: ${i.place.maps}\nWhy: ${i.reason}\nTip: ${i.place.localTip}\nSafety: ${i.place.safetyTip}`).join('\n\n')}\n\nOffline favorites: ${guidePlaces.filter(p => favorites.includes(p.id)).map(p => p.name).join(', ') || 'None'}`;
    const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob([text], { type: 'text/plain' })); a.download = `${tripName.replace(/\s+/g, '-').toLowerCase()}.txt`; a.click(); URL.revokeObjectURL(a.href);
  };
  const localGuideNote = () => {
    const firstStops = itinerary.filter(item => item.place && item.place.category !== 'coast').slice(0, 3).map(i => `${t(lang, i.slot)}: ${i.place.name}`).join(' · ');
    return `TuTour guide note: Start from ${start} and keep a ${pace} pace. Your plan is ${firstStops}. Use ${transport === 'taxi' ? 'registered taxis or ride-hail between areas' : transport === 'walking' ? 'walking in Centro during daylight and short taxi hops after dark' : 'a mix of walking by day and taxi after dark'}. Check current opening hours on Google Maps before leaving. Carry water, small cash, sun protection, and keep valuables low-profile in markets. For night plans, stay in well-lit busy streets, avoid isolated walks, and use 911 for emergencies in Mexico.`;
  };
  const askAI = async () => {
    if (!itinerary.length || loading) return; setLoading(true); setError(null); setSuccess('');
    try {
      const activeItinerary = itinerary.filter(item => item.place && item.place.category !== 'coast');
      const stopList = activeItinerary.map(item => `${item.day ? `Day ${item.day}, ` : ''}${t(lang, item.slot)}: ${item.place.name} (${item.place.category}, ${item.place.area}). ${item.note} ${item.reason} Safety: ${item.place.safetyTip}`).join('\n');
      const prompt = `Write in language code ${lang}. Oaxaca ${mode === 'multi' ? `${tripDays}-day` : '1-day'} tour guide note for style ${style}, pace ${pace}, budget ${budget}, walking ${walking}, start ${start}, transport ${transport}. Include local recommendations, arrival-ready planning advice, and safety. Under 450 words.\n${stopList}`;
      let text = '';
      if (window.genmb && window.genmb.chatbot && window.genmb.chatbot.send) {
        text = await window.genmb.chatbot.send(prompt, { systemPrompt: `You are TuTour, a practical Oaxaca local guide. Reply only in language code ${lang}.`, maxTokens: 650 });
      } else {
        text = localGuideNote();
      }
      if (!text || typeof text !== 'string') throw new Error(t(lang, 'aiRequestFailed'));
      setAiGuide(text); setSuccess(t(lang, 'aiSuccess'));
    } catch (err) {
      setAiGuide(localGuideNote());
      setError(`${err && err.message ? err.message : t(lang, 'aiRequestFailed')} ${t(lang, 'chatFallback')}`);
    } finally { setLoading(false); }
  };
  const favoritePlaces = guidePlaces.filter(p => favorites.includes(p.id));
  React.useEffect(() => {
    const styleId = 'tutour-itinerary-page-styles';
    let styleElement = document.getElementById(styleId);
    if (!styleElement) {
      styleElement = document.createElement('style');
      styleElement.id = styleId;
      document.head.appendChild(styleElement);
    }
    styleElement.textContent = itineraryPageCss;
  }, []);
  return html`<div className="itinerary-page grid gap-4 min-w-0">
    <section className="itinerary-hero-card" aria-labelledby="itinerary-title">
      <div className="itinerary-hero-image" style=${{ backgroundImage: `url(${itineraryDesignImage})` }}></div>
      <div className="itinerary-hero-content">
        <div className="itinerary-hero-topline">
          <span className="itinerary-kicker">TuTour</span>
          <span className=${`itinerary-offline-pill ${offlineReady ? 'is-ready' : ''}`}><${WifiOff} className="h-3.5 w-3.5" />${offlineReady ? t(lang, 'offlineReady') : t(lang, 'offlineMode')}</span>
        </div>
        <p className="itinerary-home-kicker"><${MapPin} className="h-5 w-5" />Oaxaca, México</p>
        <div>
          <h1 id="itinerary-title"><span>${t(lang, 'itineraryHeroBuild')}</span><span className="text-yellow-400">${t(lang, 'itineraryHeroOaxaca')}</span><span>${t(lang, 'itineraryHeroYourWay')}</span></h1>
          <div className="itinerary-wave" aria-hidden="true"><span>~</span><span>~</span><span>~</span><span>~</span><span>~</span><span>~</span><span>~</span><span>~</span></div>
          <p>${mode === 'multi' ? t(lang, 'multiDayIntro') : t(lang, 'emptyPlan')}</p>
        </div>
        <div className="itinerary-quick-stats" aria-label=${t(lang, 'itinerary')}>
          <span><${Sparkles} className="h-3.5 w-3.5" />${t(lang, 'morning')}</span>
          <span>${t(lang, 'afternoon')}</span>
          <span>${t(lang, 'night')}</span>
        </div>
        <button onClick=${build} disabled=${loading} className="focus-ring itinerary-hero-cta"><${Compass} className="h-5 w-5" />${mode === 'multi' ? t(lang, 'buildMultiDayPlan') : t(lang, 'generate')}</button>
      </div>
    </section>

    <section className="itinerary-builder-card" aria-label=${t(lang, 'itinerary')}>
      <div className="grid grid-cols-2 gap-2 rounded-[var(--radius-md)] bg-[hsl(var(--muted))] p-1" role="tablist" aria-label=${t(lang, 'itinerary')}>
        <button type="button" role="tab" aria-selected=${mode === 'single'} onClick=${() => setMode('single')} className=${`focus-ring min-h-[38px] rounded-[var(--radius-sm)] px-3 py-2 text-sm font-black ${mode === 'single' ? 'bg-[hsl(var(--card))] text-[hsl(var(--primary))] shadow-[var(--shadow-sm)]' : 'text-[hsl(var(--foreground))]'}`}>${t(lang, 'build')}</button>
        <button type="button" role="tab" aria-selected=${mode === 'multi'} onClick=${() => setMode('multi')} className=${`focus-ring min-h-[38px] rounded-[var(--radius-sm)] px-3 py-2 text-sm font-black ${mode === 'multi' ? 'bg-[hsl(var(--card))] text-[hsl(var(--primary))] shadow-[var(--shadow-sm)]' : 'text-[hsl(var(--foreground))]'}`}><${CalendarDays} className="inline h-4 w-4" /> ${t(lang, 'multiDayPlan')}</button>
      </div>
      <div className="itinerary-control-grid">${mode === 'multi' ? html`<label className="itinerary-field">${t(lang, 'tripLengthDays')}<select value=${tripDays} onChange=${e => setTripDays(Number(e.target.value))} className="focus-ring"><option value="2">2</option><option value="3">3</option><option value="4">4</option><option value="5">5</option><option value="6">6</option><option value="7">7</option></select></label>` : null}<label className="itinerary-field">${t(lang, 'travelStyle')}<select value=${style} onChange=${e => setStyle(e.target.value)} className="focus-ring"><option value="relaxed">${t(lang, 'relaxedStyle')}</option><option value="foodie">${t(lang, 'foodieStyle')}</option><option value="culture">${t(lang, 'cultureStyle')}</option><option value="nature">${t(lang, 'natureStyle')}</option><option value="family">${t(lang, 'familyStyle')}</option><option value="budget">${t(lang, 'budgetStyle')}</option></select></label><label className="itinerary-field">${t(lang, 'pace')}<select value=${pace} onChange=${e => setPace(e.target.value)} className="focus-ring"><option value="relaxed">${t(lang, 'relaxedStyle')}</option><option value="balanced">${t(lang, 'balanced')}</option><option value="packed">${t(lang, 'packed')}</option></select></label><label className="itinerary-field">${t(lang, 'budget')}<select value=${budget} onChange=${e => setBudget(e.target.value)} className="focus-ring"><option value="low">${t(lang, 'value')}</option><option value="mid">${t(lang, 'midRange')}</option><option value="high">${t(lang, 'premium')}</option><option value="luxury">${t(lang, 'luxury')}</option></select></label><label className="itinerary-field">${t(lang, 'walkingTolerance')}<select value=${walking} onChange=${e => setWalking(e.target.value)} className="focus-ring"><option value="low">${t(lang, 'low')}</option><option value="medium">${t(lang, 'medium')}</option><option value="high">${t(lang, 'high')}</option></select></label><label className="itinerary-field">${t(lang, 'startLocation')}<input value=${start} onInput=${e => setStart(e.target.value)} className="focus-ring" /></label><label className="itinerary-field">${t(lang, 'transportPref')}<select value=${transport} onChange=${e => setTransport(e.target.value)} className="focus-ring"><option value="walking">${t(lang, 'walking')}</option><option value="taxi">${t(lang, 'taxi')}</option><option value="mixed">${t(lang, 'mixed')}</option></select></label></div>
      <div className="grid gap-2"><span className="text-xs font-black">${t(lang, 'preferredCategories')}</span><div className="itinerary-chip-row">${visibleCategories.map(cat => html`<button key=${cat} type="button" onClick=${() => toggleCat(cat)} className=${`focus-ring itinerary-chip ${cats.includes(cat) ? 'is-selected' : ''}`}>${t(lang, cat)}</button>`)}</div></div>
      ${favoritePlaces.length ? html`<div className="grid gap-2"><span className="text-xs font-black">${t(lang, 'mustSeeSaved')}</span><div className="itinerary-chip-row">${favoritePlaces.map(p => html`<button key=${p.id} type="button" onClick=${() => toggleMust(p.id)} className=${`focus-ring itinerary-chip favorite ${mustSee.includes(p.id) ? 'is-selected' : ''}`}>${p.emoji} ${p.name}</button>`)}</div></div>` : null}
      <button onClick=${build} disabled=${loading} className="focus-ring itinerary-generate-button"><${RefreshCw} className="h-4 w-4" />${itinerary.length ? t(lang, 'refresh') : (mode === 'multi' ? t(lang, 'buildMultiDayPlan') : t(lang, 'generate'))}</button>
    </section>

    ${Object.keys(grouped).length === 0 ? html`<div className="itinerary-empty-card"><div className="text-5xl">🧭</div><p>${mode === 'multi' ? t(lang, 'multiDayIntro') : t(lang, 'emptyPlan')}</p></div>` : html`<div className="grid gap-4">${Object.keys(grouped).map(day => html`<section key=${day} className="itinerary-day-section"><h2><span>${t(lang, 'day')} ${day}</span></h2><div className="itinerary-place-grid">${grouped[day].map((item, index) => html`<div key=${`${day}-${item.slot}-${item.place.id}`} className="itinerary-place-item"><span className="itinerary-slot-label">${index + 1}. ${t(lang, item.slot)}</span><${PlaceCard} place=${placeForPlacesPage(item.place)} /></div>`)}</div></section>`)}</div><div className="itinerary-save-panel"><input value=${tripName} onInput=${e => setTripName(e.target.value)} aria-label=${t(lang, 'tripName')} className="focus-ring" /><input type="date" value=${tripDate} onInput=${e => setTripDate(e.target.value)} className="focus-ring" /><button onClick=${saveCurrent} className="focus-ring itinerary-save-button"><${Save} className="h-4 w-4" />${t(lang, 'saveTrip')}</button><button onClick=${downloadPlan} className="focus-ring itinerary-download-button"><${Download} className="h-4 w-4" />${t(lang, 'downloadPlan')}</button></div><section className="itinerary-ai-card"><div className="flex flex-col gap-3 md:flex-row md:items-start md:justify-between"><div><h2 className="flex items-center gap-1.5"><${Bot} className="h-5 w-5 text-[hsl(var(--primary))]" />${t(lang, 'aiGuideTitle')}</h2><p className="mt-1.5 text-sm text-[hsl(var(--muted-foreground))]">${t(lang, 'aiGuideText')}</p></div><button onClick=${askAI} disabled=${loading || !itinerary.length} className="focus-ring itinerary-ai-button">${loading ? html`<${Loader2} className="h-4 w-4 animate-spin" />${t(lang, 'aiLoading')}` : html`<${Sparkles} className="h-4 w-4" />${aiGuide ? t(lang, 'aiRegenerate') : t(lang, 'aiGenerate')}`}</button></div>${error ? html`<p role="alert" className="mt-3 rounded-[var(--radius-md)] bg-[hsl(var(--destructive)/0.12)] p-2.5 text-sm font-bold text-[hsl(var(--destructive))]">${error}</p>` : null}${success ? html`<p className="mt-3 rounded-[var(--radius-md)] bg-[hsl(var(--secondary)/0.12)] p-2.5 text-sm font-bold text-[hsl(var(--secondary))]">${success}</p>` : null}<div className=${`mt-3 whitespace-pre-line rounded-[var(--radius-md)] p-4 text-sm font-semibold leading-relaxed ${aiGuide ? 'bg-[hsl(var(--muted))]' : 'border border-dashed border-[hsl(var(--border))] bg-[hsl(var(--card)/0.75)] text-[hsl(var(--muted-foreground))]'}`}>${aiGuide || t(lang, 'aiPlaceholder')}</div></section>`}
    <section className="itinerary-saved-card"><h2><${Heart} className="h-5 w-5 text-[hsl(var(--destructive))]" />${t(lang, 'savedTripsFavorites')}</h2><p>${t(lang, 'guestSaved')}</p><div className="itinerary-saved-grid">${favoritePlaces.length ? favoritePlaces.map(p => html`<a key=${p.id} href=${p.maps} target="_blank" rel="noreferrer" className="focus-ring itinerary-saved-link">${p.emoji} ${p.name}</a>`) : html`<p className="text-sm text-[hsl(var(--muted-foreground))]">${t(lang, 'noSaved')}</p>`}${savedItineraries.map(s => html`<div key=${s.id} className="itinerary-saved-trip"><input value=${s.name || t(lang, 'savedTrip')} onInput=${e => renameItinerary(s.id, e.target.value)} className="focus-ring" /><span>${s.date || ''} · ${s.itinerary.filter(i => i.place && i.place.category !== 'coast').map(i => `${i.day ? `${t(lang, 'day')} ${i.day}: ` : ''}${i.place.name}`).join(' → ')}</span><div className="flex gap-2"><button onClick=${() => setItinerary(s.itinerary.filter(i => i.place && i.place.category !== 'coast'))} className="focus-ring rounded-[var(--radius-md)] bg-[hsl(var(--primary))] px-2 py-1 text-xs text-[hsl(var(--primary-foreground))]">${t(lang, 'view')}</button><button onClick=${build} className="focus-ring rounded-[var(--radius-md)] bg-[hsl(var(--accent))] px-2 py-1 text-xs text-black">${t(lang, 'regenerate')}</button><button onClick=${() => deleteItinerary(s.id)} className="focus-ring rounded-[var(--radius-md)] bg-[hsl(var(--destructive)/0.12)] px-2 py-1 text-xs text-[hsl(var(--destructive))]"><${Trash2} className="h-3.5 w-3.5" /><span className="sr-only">${t(lang, 'savedTrip')}</span></button></div></div>`)}</div></section>
  </div>`;
}
